<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup
    x:Class="Packages.MAUI.App.Views.Popups.UserCompletedCodePopup"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:viewModel="clr-namespace:Packages.MAUI.App.ViewModels.Popups.UserCompletedCode"
    Padding="0"
    x:DataType="viewModel:UserCompletedCodeViewModel">

    <VerticalStackLayout
        Padding="30,40,30,30"
        BackgroundColor="White"
        Spacing="10"
        VerticalOptions="Center">

        <Label
            FontAttributes="Bold"
            FontSize="22"
            Text="Code completed"
            TextColor="Black" />

        <Label
            FontSize="18"
            Text="This is the code you submitted:"
            TextColor="Black" />

        <Label
            Margin="0,20,0,30"
            FontAttributes="Bold"
            FontSize="28"
            HorizontalOptions="Center"
            Text="{Binding Code}"
            TextColor="Black" />

        <Button
            BackgroundColor="#027AFF"
            Command="{Binding CloseCommand}"
            CornerRadius="10"
            FontAttributes="Bold"
            HeightRequest="50"
            Text="Nice :)"
            TextColor="White" />
    </VerticalStackLayout>
</toolkit:Popup>