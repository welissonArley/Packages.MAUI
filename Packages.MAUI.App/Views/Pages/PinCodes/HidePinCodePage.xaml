<?xml version="1.0" encoding="utf-8" ?>
<pinCodeAuthorization:CodePage
    x:Class="Packages.MAUI.App.Views.Pages.PinCodes.HidePinCodePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:codeViewer="clr-namespace:PinCodes.Authorization.Views.Components.CodeViewers;assembly=PinCodes.Authorization.Maui"
    xmlns:fontFamily="clr-namespace:Packages.MAUI.App.Constants"
    xmlns:keyboardViewer="clr-namespace:PinCodes.Authorization.Views.Components.Keyboards;assembly=PinCodes.Authorization.Maui"
    xmlns:pinCodeAuthorization="clr-namespace:PinCodes.Authorization.Views.Pages;assembly=PinCodes.Authorization.Maui"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    x:Name="PagePinCode"
    CallbackCodeFinished="{Binding BindingContext.UserCompletedCodeCommand, Source={x:Reference PagePinCode}}">

    <pinCodeAuthorization:CodePage.Header>
        <VerticalStackLayout Margin="0,20,0,40" Spacing="5">
            <Label
                FontFamily="{Static fontFamily:FontFamily.MAIN_FONT_BOLD}"
                FontSize="36"
                HorizontalOptions="Center"
                HorizontalTextAlignment="Center"
                Text="Verify Phone" />

            <Label
                FontFamily="{Static fontFamily:FontFamily.MAIN_FONT_LIGHT}"
                HorizontalOptions="Center"
                Text="code has been sent to +351 912345678" />
        </VerticalStackLayout>
    </pinCodeAuthorization:CodePage.Header>

    <pinCodeAuthorization:CodePage.SubHeader>
        <VerticalStackLayout Margin="0,30,0,20" HorizontalOptions="Center">
            <Label
                Margin="0,0,0,10"
                FontFamily="{Static fontFamily:FontFamily.MAIN_FONT_REGULAR}"
                FontSize="16"
                Text="Didn’t get the verification code?" />

            <VerticalStackLayout
                Padding="0,5,0,5"
                HeightRequest="40"
                HorizontalOptions="Center">
                <VerticalStackLayout.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding BindingContext.ResendCodeCommand, Source={x:Reference PagePinCode}}" />
                </VerticalStackLayout.GestureRecognizers>
                <Label
                    FontFamily="{Static fontFamily:FontFamily.MAIN_FONT_BOLD}"
                    HorizontalOptions="Center"
                    Text="Resend code"
                    TextColor="{AppThemeBinding Light={StaticResource HIGHLIGHT_COLOR_LIGHT},
                                                Dark={StaticResource HIGHLIGHT_COLOR_DARK}}" />
            </VerticalStackLayout>
        </VerticalStackLayout>
    </pinCodeAuthorization:CodePage.SubHeader>

    <pinCodeAuthorization:CodePage.CodeViewer>
        <codeViewer:HideCodeViewer CodeColor="Yellow" CodeStrokeColor="Red">
            <codeViewer:HideCodeViewer.ShapeViewer>
                <RoundRectangle
                    CornerRadius="10"
                    Fill="Transparent"
                    HeightRequest="50"
                    Stroke="{AppThemeBinding Light={StaticResource PRIMARY_COLOR_LIGHT},
                                             Dark={StaticResource PRIMARY_COLOR_DARK}}"
                    StrokeThickness="2"
                    WidthRequest="50" />
            </codeViewer:HideCodeViewer.ShapeViewer>
        </codeViewer:HideCodeViewer>
    </pinCodeAuthorization:CodePage.CodeViewer>

    <pinCodeAuthorization:CodePage.Keyboard>
        <keyboardViewer:KeyboardViewer ColumnSpacing="40" RowSpacing="20">
            <keyboardViewer:KeyboardViewer.ShapeViewer>
                <Button
                    BackgroundColor="{AppThemeBinding Light={StaticResource KEYBOARD_COLOR_LIGHT},
                                                      Dark={StaticResource KEYBOARD_COLOR_DARK}}"
                    CornerRadius="20"
                    FontFamily="{Static fontFamily:FontFamily.MAIN_FONT_BOLD}"
                    FontSize="24"
                    HeightRequest="80"
                    TextColor="{AppThemeBinding Light={StaticResource PRIMARY_COLOR_LIGHT},
                                                Dark={StaticResource PRIMARY_COLOR_DARK}}"
                    WidthRequest="80" />
            </keyboardViewer:KeyboardViewer.ShapeViewer>

            <keyboardViewer:KeyboardViewer.BackspaceViewer>
                <ImageButton
                    Padding="{OnPlatform Default=15,
                                         Android=20,
                                         iOS=22}"
                    BackgroundColor="Transparent"
                    Source="illustration_delete.png">
                    <ImageButton.Behaviors>
                        <toolkit:IconTintColorBehavior TintColor="{AppThemeBinding Light={StaticResource PRIMARY_COLOR_LIGHT}, Dark={StaticResource PRIMARY_COLOR_DARK}}" />
                    </ImageButton.Behaviors>
                </ImageButton>
            </keyboardViewer:KeyboardViewer.BackspaceViewer>

            <keyboardViewer:KeyboardViewer.LeftSideButtonShapeViewer>
                <ImageButton
                    Padding="{OnPlatform Default=15,
                                         Android=20,
                                         iOS=22}"
                    BackgroundColor="Transparent"
                    Command="{Binding BindingContext.FaceIdCommand, Source={x:Reference PagePinCode}}"
                    Source="illustration_faceid.png">
                    <ImageButton.Behaviors>
                        <toolkit:IconTintColorBehavior TintColor="{AppThemeBinding Light={StaticResource PRIMARY_COLOR_LIGHT}, Dark={StaticResource PRIMARY_COLOR_DARK}}" />
                    </ImageButton.Behaviors>
                </ImageButton>
            </keyboardViewer:KeyboardViewer.LeftSideButtonShapeViewer>
        </keyboardViewer:KeyboardViewer>
    </pinCodeAuthorization:CodePage.Keyboard>

</pinCodeAuthorization:CodePage>